<div class="input-group">
    <input
        class="form-control"
        placeholder="{{$placeholder}}"
        type="number"
        max="{{$max}}"
        min="{{$min}}"
        value="{{$value}}"
        name="{{$name}}"
    />
    <div class="input-group-append">
        <div class="input-group-text">
            {{$text}}
        </div>
    </div>
</div>
